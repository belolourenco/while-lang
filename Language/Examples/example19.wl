x := y;
while x <= 0 do {x > 0}
  x := y - 1;
  y := x * z;
  while x <= 0 do {x > 0}
    x := y - 1;
    y := x * z
  od;
  while x <= 0 do {x > 0}
    x := y - 1;
    y := x * z;
    while x <= 0 do {x > 0}
      x := y - 1;
      y := x * z
    od
  od
od;
while x <= 0 do {x > 0}
  x := y - 1;
  y := x * z;
  while x <= 0 do {x > 0}
    x := y - 1;
    y := x * z
  od;
  while x <= 0 do {x > 0}
    x := y - 1;
    y := x * z;
    while x <= 0 do {x > 0}
      x := y - 1;
      y := x * z
    od
  od
od;
x := y;
while x <= 0 do {x > 0}
  x := y - 1;
  y := x * z;
  while x <= 0 do {x > 0}
    x := y - 1;
    y := x * z
  od;
  while x <= 0 do {x > 0}
    x := y - 1;
    y := x * z;
    while x <= 0 do {x > 0}
      x := y - 1;
      y := x * z
    od
  od
od;
while x <= 0 do {x > 0}
  x := y - 1;
  y := x * z;
  while x <= 0 do {x > 0}
    x := y - 1;
    y := x * z
  od;
  while x <= 0 do {x > 0}
    x := y - 1;
    y := x * z;
    while x <= 0 do {x > 0}
      x := y - 1;
      y := x * z
    od
  od
od;
x := y;
while x <= 0 do {x > 0}
  x := y - 1;
  y := x * z;
  while x <= 0 do {x > 0}
    x := y - 1;
    y := x * z
  od;
  while x <= 0 do {x > 0}
    x := y - 1;
    y := x * z;
    while x <= 0 do {x > 0}
      x := y - 1;
      y := x * z
    od
  od
od;
while x <= 0 do {x > 0}
  x := y - 1;
  y := x * z;
  while x <= 0 do {x > 0}
    x := y - 1;
    y := x * z
  od;
  while x <= 0 do {x > 0}
    x := y - 1;
    y := x * z;
    while x <= 0 do {x > 0}
      x := y - 1;
      y := x * z
    od
  od
od;
x := y;
while x <= 0 do {x > 0}
  x := y - 1;
  y := x * z;
  while x <= 0 do {x > 0}
    x := y - 1;
    y := x * z
  od;
  while x <= 0 do {x > 0}
    x := y - 1;
    y := x * z;
    while x <= 0 do {x > 0}
      x := y - 1;
      y := x * z
    od
  od
od;
while x <= 0 do {x > 0}
  x := y - 1;
  y := x * z;
  while x <= 0 do {x > 0}
    x := y - 1;
    y := x * z
  od;
  while x <= 0 do {x > 0}
    x := y - 1;
    y := x * z;
    while x <= 0 do {x > 0}
      x := y - 1;
      y := x * z
    od
  od
od